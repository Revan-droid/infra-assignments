---
apiVersion: v1
kind: Service
metadata:
  name: config-service
  namespace: config-service
  labels:
    app: config-service
spec:
  selector:
    app: config-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
